<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.cs.scu.dao.TaskInfoDao">

    <resultMap type="taskBean" id="taskList">
        <result column="taskId" property="taskId"/>
        <result column="taskName" property="taskName"/>
        <result column="startTime" property="startTime"/>
        <result column="finishTime" property="finishTime"/>
        <result column="taskType" property="taskType"/>
        <result column="taskStatus" property="taskStatus"/>
        <result column="taskParam" property="taskParam"/>
    </resultMap>

    <select id="getTaskCount" resultType="java.lang.Integer">
         select count(1) from task
    </select>

    <select id="getTaskInfo" resultType="taskBean">
        select taskId,taskName,startTime,finishTime,taskType,taskStatus,taskParam from task
    </select>
    <select id="getTaskById" resultType="edu.cs.scu.bean.TaskBean"></select>

</mapper>
    